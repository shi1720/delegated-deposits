# Data and numerical implementation amendment

23 September 2026, after theoretical parameter sweeps and before balance-sheet scenario analysis.

The FDIC endpoint also returns noninsured trust companies. Add the published insurance flags to each snapshot and restrict the main sample to insured banks and savings institutions (excluding insured foreign branches, BKCLASS=OI). Report the exclusion flow. This correction enforces the intended sample rather than selecting on outcomes.

The first Python 3.9 / NumPy 2.0 environment emitted spurious BLAS floating-point warnings even on bounded finite matrix products. Rebuilt the isolated environment using Python 3.12 and current packages; the entire test suite passes with warnings treated as errors. The lockfile records the actual versions. No warnings are suppressed.

The primary Bernstein degree is 512 with a 1,001-node moment grid. Retain and report numerical gaps; exact rational verification is performed for the 18 central fixed-cash cases. A finite-exchangeable comparator was added after deriving the conditional-independence results to expose the effect of that maintained assumption. This is an explicit sensitivity extension.
